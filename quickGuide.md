# 使用说明

**依赖安装**：

```bash
pip install -r requirements.txt
```

---

## 主脚本

### `msykanswer.py`

**简介**：提供登录、作业答案获取与提交、文件下载、PPT 转 PDF

**主要功能**：

| 功能 | 菜单选项 | 说明 |
|------|----------|------|
| 作业获取答案 | `1` | 输入作业 ID，自动获取答案支持一键提交 |
| 跑作业 ID | `2` | 按范围遍历查询作业信息 |
| 切换账号 | `3` | 清除缓存，重新登录 |
| 退出 | `4` | 退出程序 |
| 消息系统 | `5`（可选） | 需在 `settings.ini` 中启用 |
| 学习圈系统 | `6`（可选） | 需在 `settings.ini` 中启用 |

---

### `msykanswer1221.py`

**用途**：上述脚本的更新版本，集成 `passwordTry` 和 `oss_tool2`，相比 `msykanswer.py` 增加了密码爆破、成绩查询、OSS 文件篡改等功能。此版本不读取 `settings.ini`，且消息/学习圈功能默认注释。

**相比 `msykanswer.py` 额外菜单**：

| 功能 | 菜单选项 | 说明 |
|------|----------|------|
| 爬密码 | `5` | 批量尝试学生/教师密码 |
| 获取近期作业班均分 | `6` | 查看班级平均分与个人分数对比 |
| 获取 OSS 临时凭据及篡改文件 | `7` | 上传/替换 OSS 文件 |
| 获取历史作业列表 | `8` | 显示本学期所有作业 |
| 测试 | `9` | 调用测试功能 |

**使用方式**：

```bash
python msykanswer1221.py
```

---

## 其他脚本

### `HiddenAnswerMsyk.py` — 隐藏版自动答题工具

**用途**：精简的答案获取和提交工具。无需交互，启动自动获取得最近的作业自动提交答案。

**使用方式**：

```bash
python HiddenAnswerMsyk.py
```

**条件**：必须先准备 `ProfileCache.txt`。

---

### `passwordTry.py` — 密码批量尝试工具

**用途**：密码批量尝试，遍历班级号和学生号发现弱密码账号。

**使用方式**：

- **方式一**（通过 msykanswer1221.py）：
  ```bash
  python msykanswer1221.py
  # 在主菜单选择 "5.爬密码"
  ```
  然后输入用户类型（`1`=学生，其他=教师）和要尝试的密码。

- **方式二**（直接调用测试函数）：
  ```bash
  python msykanswer1221.py
  # 在主菜单选择 "9.测试"
  ```

**学生账号命名规则**：`sdfz{202301-202313}{00-60}`

**教师账号命名规则**：`sdfz{180000-180256}`

**输出**：成功登录的账号会实时打印，并汇总在 `defultUserList` 列表中。

---

### `oss_tool2.py` — 阿里云 OSS 操作工具

**用途**：对使用的阿里云 OSS 增删改。

**使用方式**：

- **通过 msykanswer1221.py 调用**：
  ```bash
  python msykanswer1221.py
  # 选择 "7.获取oss临时凭据及篡改文件"
  # 程序自动获取 STS 凭证，并从作业材料文件中解析 OSS 路径
  ```
  
---

### `msyk_message.py` — 消息系统模块

**用途**：消息系统扩展模块，提供消息浏览、详情查看和附件下载功能。需在主程序 `msykanswer.py` 的 `settings.ini` 中设置 `enable_message = True` 启用。

**附件下载**：查看消息详情时，如果消息包含附件可选择下载。附件会自动从CDN下载到当前工作目录。

**使用方式**：

1. 运行 `msykanswer.py`
2. 在主菜单选择 `5.消息系统`

---

### `msyk_learning_circle.py` — 学习圈系统模块

**用途**：学习圈模块，提供提问、查看问题和讨论功能。需在主程序 `msykanswer.py` 的 `settings.ini` 中设置 `enable_learning_circle = True` 启用。

**菜单选项**：

- `1`：查看问题详情
- `2`：提问（向指定教师提交问题）
- `3`：添加聊天内容（在已有问题下回复）
- `4`：返回主菜单

**使用方式**：

1. 运行 `msykanswer.py`
2. 在主菜单选择 `6.学习圈系统`

---

## 配置文件

### `settings.ini`
